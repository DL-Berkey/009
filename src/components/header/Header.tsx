import ThemeButton from "./ThemeButton";

const Header = () => {
    return (
        <header className="flex">
            <div>
                <span>009</span>
            </div>
            <div>
                <ThemeButton />
            </div>
        </header>
    );
};

export default Header;
